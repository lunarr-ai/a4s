import 'package:flutter/material.dart';

class AgentCardWidget extends StatelessWidget {
  const AgentCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
